# IBVAP - Intelligent Border Video Analytics Platform
# Core runtime dependencies (Python 3.14+).
#
# Install with:  pip install -r requirements.txt

ultralytics==8.4.130
opencv-python==5.0.0.93
torch==2.13.0
torchvision==0.28.0
numpy==2.5.2
lap==0.5.13

# ANPR OCR engine (v1.7.2 pulls opencv-python-headless; keep opencv-python
# installed as well so cv2 stays on the same wheel for the CV pipeline).
easyocr==1.7.2